package com.indigo.booking.exception;

public enum ErrorCode {
    INSUFFICIENT_SEATS,
    BOOKING_EXPIRED,
    INVALID_BOOKING_STATE,
    RESOURCE_NOT_FOUND,
    VALIDATION_ERROR,
    INTERNAL_SERVER_ERROR
}
